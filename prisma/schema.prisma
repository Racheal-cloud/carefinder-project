
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model hospital {
  id       Int      @id 
  name     String
  address  String
  phone    String
}
